node_modules
.vercel
.next
tsconfig.tsbuildinfo
.env*.local

# OpenNext
.open-next

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# OpenNext build artifact (causes worker deploy precedence bug)
wrangler.jsonc

# Local env file for Edge Function development (never commit)
supabase/functions/.env

# META-1 agent drafts (v1 con problemas conocidos — no commitear hasta v2)
.claude/agents/

# QA screenshots locales (artefactos de sesiones visuales)
_legacy/screenshots/
